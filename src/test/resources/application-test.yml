spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:league_soccer_test}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:2005}
  flyway:
    clean-disabled: false

jwt:
  secret: test-secret-key-only-for-tests-not-for-production-use
  expiration-ms: 3600000
  refresh-expiration-ms: 86400000